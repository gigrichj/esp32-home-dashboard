#pragma once
// Bump this every time firmware is rebuilt, so the screen always shows
// which version is actually running — makes it obvious whether a fresh
// flash actually took effect.
#define FIRMWARE_VERSION "v14"
